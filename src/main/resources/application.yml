# 开发配置
spring:
  # 数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/mybatislearn?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
# mybatis配置
mybatis:
  type-aliases-package: com.example.demo.model
  mapper-locations: classpath:mapper/*.xml